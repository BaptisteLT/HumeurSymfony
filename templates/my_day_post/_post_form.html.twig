{{ form_start(form) }}
    <div class="mb-3">
        {{ form_row(form.title, {'attr': {'class': 'form-control'}}) }}
    </div>
        {{ form_row(form.description, {'attr': {'class': 'form-control'}}) }}
    <div class="mb-3">
        {{ form_row(form.private, {'attr': {'class': 'form-check-input'}}) }}
    </div>
    <div class="mb-3">
        {{ form_row(form.save, {'attr': {'class': 'btn btn-primary'}}) }}
    </div>
{{ form_end(form) }}